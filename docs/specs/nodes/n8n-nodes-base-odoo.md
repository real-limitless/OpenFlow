# Factory job – SPEC (clean‑room half A)

**Model:** `xai/grok-4.5`  
**Node type:** `n8n-odes-base.odoo`  
**Batch:** `queue`  
**Cycle:** `1` of `4`

---

## Sources  *(Public docs only)*
- https://docs.n8n.io/integrations/builtin/nodes/n8n-nodes-base-odoo/  *(configuration & parameters)
- https://docs.n8n.io/integrations/builtin/nodes/n8n-nodes-base/essentials/reference/  *(common node structure)

---

## Wire format
- **Type string:** `n8n-nodes-base-odoo`
- **Inputs:** A JSON payload representing the data to be sent to Odoo (e.g., model, fields, values).
- **Outputs:** The response from Odoo, typically containing a `data` field with the result of the operation.
- **Credentials:** OAuth2 or Basic Auth to authenticate against an Odoo instance.

---

## Parameters  *(abstracted)*
- **Operation** (`string`): The Odoo RPC operation to perform – options: `create`, `update`, `call`, `read`, `search`.  (Inferred from public docs.)
- **Model** (`string`): The Odoo model name the operation applies to.
- **Data** (`object`): The payload containing the fields and values to be processed.
- **Options** (`object`): Additional runtime settings (e.g., `dateCompute`, `context`).  Included only when required for interoperability.

---

## Runtime behavior
1. **Input validation** – The node verifies that `operation` is one of the permitted values and that `model` matches an Odoo model.
2. **Parameter mapping** – Abstract parameters (`operation`, `model`, `data`) are transformed into a single JavaScript object that represents the request to the Odoo RPC endpoint.
3. **HTTP call** – An authenticated HTTP POST is issued to the Odoo RPC URL (`/xmlrpc/2/object`). The exact request body follows n8n’s generic node contract: `{ function: op, args: [model, data] }`.
4. **Response handling** – The raw JSON response is parsed. If `data` contains `recordId` it is emitted as `recordId`; otherwise the full response is emitted under `data`.
5. **Error handling** – Errors from Odoo (e.g., permission denied, invalid model) trigger an `error` output with a structured `statusCode` and `message` field, following the n8n error model.

---

## Acceptance tests
1. **Create record** – Input `{ operation: "create", model: "res.partner", data: { name: "Acme", email: "acme@example.com" } }`. Expected output contains `recordId` = "3".
2. **Update record** – Input `{ operation: "update", model: "res.partner", data: { id: "3", name: "Acme Inc." } }`. Expected output shows updated `name`.
3. **Read record** – Input `{ operation: "read", model: "res.partner", data: { ids: [3], fields: ["name", "email"] } }`. Expected output includes fields `name` and `email`.
4. **Search record** – Input `{ operation: "search", model: "res.partner", data: { domain: [["name", "like", "Acme"]], offset: 0, limit: 10 } }`. Expected output returns at least one `id`.
5. **Call RPC method** – Input `{ operation: "call", model: "ir.model.fields", data: { model_name: "res.partner", field_name: "name" } }`. Expected output contains the field definition.

---

## Gaps / confidence
- **Parameter defaults** – The public docs indicate `options` may contain `dateCompute` and `context`, but exact defaults are not detailed; therefore they are omitted unless required by a specific workflow.
- **Error mapping** – Odoo errors are translated to the generic n8n error shape, which is assumed to be consistent with other nodes; no detailed mapping is provided.
- **Concurrency** – The docs do not specify rate‑limit handling; the node will defer such concerns to the underlying HTTP layer.
Overall confidence: **High** for core CRUD operations; **Medium** for nuanced RPC calls and optional options.

---

## OpenFlow mapping
- **Definition group:** `odoo`
- **Intended executor filename:** `odooExecutor.ts`  *(to be created in `src/executor/odooExecutor.ts`)*

---

*Generated by the OpenFlow factory clean‑room SPEC agent. No implementation code was produced.*
